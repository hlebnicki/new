<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @jerry.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @jerry.description %>
</p>

<p>
  <strong>Picture:</strong>
  <%= image_tag(@jerry.picture_url, width: 100) if @jerry.picture.present? %>
</p>

<%= link_to 'Edit', edit_jerry_path(@jerry) %> |
<%= link_to 'Back', jerries_path %>
